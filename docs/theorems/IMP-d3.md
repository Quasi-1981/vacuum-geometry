# IMPORT d≥3: spatial dimensionality is a declared boundary, not derived

**Kind:** input · **Status:** named · **Address:** §0 · basis.md §15(v) · K1 F-1(b) · ruling 2026-07-28

*A node of the prime graph. Status is carried by the graph and is not restated here.*

## INPUT

None. This node is a **root** of the graph: it is assumed, not derived.

## FUNCTION

The spatial dimensionality d≥3 is an IMPORT (multiplicity 0): a declared boundary premise of the imports class — not a root, not a numeric handle, not derived. It feeds p≥3 in T5 (the cell itself is proved for all d and does not fix d). The d=3 fingerprints across the canon (the mark at d=3, su(3) uniqueness, n_f=d, the non-splitting of (3,1)) are fingerprints, not a selector; the bound d+1≤4 is measured twice independently [S1059B, S1063-T3], with the terminal abelian step of S_{d+1} as the candidate common ancestor [S1102, J-0531].

> **Boundary:** Import, multiplicity 0, outside the roots; the d=3-disposition front stays open. Reopens if that front closes by deriving d — the node is then promoted import→theorem by the AX-indef precedent. [K1 F-1(b), court of 2026-07-28, author's word: norm + import]

## OUTPUT

Makes the d≥3 premise an explicit graph node, so the public count reads honestly: four roots, one named import (d≥3, open front), one named norm (minimality).

**Consumed by:** [Minimality implies (3,1): p>=3 and q>=1 implies n>=4; the minimum is uniquely (3,1); (2,2) fails](T5.md)

## Measurements

*Measurement lives in an internal record (this node has no probe in the public port yet).*

---

[← all nodes](index.md)
