# The 'channel law' form is refuted; leg-1 measured across families, dim c=Sum m^2+k(k-1)/2

**Kind:** theorem · **Status:** measured · **Address:** §3.arc · S918+S919+S925+S927+S928+S929+S930

*A node of the prime graph. Status is carried by the graph and is not restated here.*

## INPUT

| premise | kind | status |
|:--|:--|:--|
| [The carrier is three-layered (combinatorial closure equals Lie closure)](T12.md) | theorem | measured |

## FUNCTION

The previously proposed 'channel law' formulation is refuted; leg-1 is instead measured across parameter families with dimension c = Sum m^2 + k(k-1)/2.

> **Boundary:** Families with n<=6, m<=2; imported status, multiplicity 0.

## OUTPUT

Rules out the channel-law hypothesis and supplies the verified dimension formula used by the nilpotent taxonomy.

**Consumed by:** [The map of nonabelian kernels is closed: 4 classes; radical dichotomy abelian vs Heisenberg](T17.md)

## PROOF COMPOSITION

The proof consumes one group of premises:

- [The carrier is three-layered (combinatorial closure equals Lie closure)](T12.md)

**Independent witness groups:** 1

## Measurements

**S918**

??? note "Probe code (`S918.py`)"
    ```python
    --8<-- "src/probe/S918.py"
    ```

??? note "Full run log (`S918_run.log`)"
    ```text
    --8<-- "src/probe/S918_run.log"
    ```

**S919**

??? note "Probe code (`S919.py`)"
    ```python
    --8<-- "src/probe/S919.py"
    ```

**Result:** exit=0

??? note "Full run log (`S919_run.log`)"
    ```text
    --8<-- "src/probe/S919_run.log"
    ```

**S925**

??? note "Probe code (`S925.py`)"
    ```python
    --8<-- "src/probe/S925.py"
    ```

**Result:** asserts_passed=510 · FAIL=0

??? note "Full run log (`S925_run.log`)"
    ```text
    --8<-- "src/probe/S925_run.log"
    ```

**S927**

??? note "Probe code (`S927.py`)"
    ```python
    --8<-- "src/probe/S927.py"
    ```

**Result:** asserts_passed=229 · FAIL=0

??? note "Full run log (`S927_run.log`)"
    ```text
    --8<-- "src/probe/S927_run.log"
    ```

**S928**

??? note "Probe code (`S928.py`)"
    ```python
    --8<-- "src/probe/S928.py"
    ```

**Result:** asserts_passed=550 · FAIL=0

??? note "Full run log (`S928_run.log`)"
    ```text
    --8<-- "src/probe/S928_run.log"
    ```

**S929_1**

??? note "Probe code (`S929_1.py`)"
    ```python
    --8<-- "src/probe/S929_1.py"
    ```

??? note "Full run log (`S929_1_run.log`)"
    ```text
    --8<-- "src/probe/S929_1_run.log"
    ```

**S929_2**

??? note "Probe code (`S929_2.py`)"
    ```python
    --8<-- "src/probe/S929_2.py"
    ```

**Result:** asserts_passed=2521 · FAIL=0

??? note "Full run log (`S929_2_run.log`)"
    ```text
    --8<-- "src/probe/S929_2_run.log"
    ```

**S930**

??? note "Probe code (`S930.py`)"
    ```python
    --8<-- "src/probe/S930.py"
    ```

**Result:** asserts_passed=37 · FAIL=0

??? note "Full run log (`S930_run.log`)"
    ```text
    --8<-- "src/probe/S930_run.log"
    ```


---

[← all nodes](index.md)
