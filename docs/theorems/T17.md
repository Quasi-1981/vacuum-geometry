# The map of nonabelian kernels is closed: 4 classes; radical dichotomy abelian vs Heisenberg

**Kind:** theorem · **Status:** measured · **Address:** §3.arc · S933+S935+S937+S940

*A node of the prime graph. Status is carried by the graph and is not restated here.*

## INPUT

| premise | kind | status |
|:--|:--|:--|
| [The 'channel law' form is refuted; leg-1 measured across families, dim c=Sum m^2+k(k-1)/2](T13.md) | theorem | measured |
| [Taxonomy of nilpotents; deep iff Witt index>=2; (3,1) immune beyond the imported Witt boundary](T14.md) | theorem | measured |
| [Shift: brackets forced within so(p,q) semidirect R^n; construct-class lineage measured (S943)](T16.md) | theorem | measured |

## FUNCTION

The classification of nonabelian kernels is complete, comprising 4 classes, with radicals splitting dichotomously into abelian and Heisenberg types.

> **Boundary:** Enumeration for n<=6; the cast-status is lifted by T18.

## OUTPUT

Closes the kernel classification, providing the 4-class map used as input to the universal ∀n mechanism.

**Consumed by:** no node lists this one among its premises. (That is a statement about the `deps` edges, which record ancestry to the forcing root rather than a chain — not a claim that nothing follows from it.)

## PROOF COMPOSITION

The proof consumes one group of premises:

- [Taxonomy of nilpotents; deep iff Witt index>=2; (3,1) immune beyond the imported Witt boundary](T14.md) · [Shift: brackets forced within so(p,q) semidirect R^n; construct-class lineage measured (S943)](T16.md)

**Independent witness groups:** 1

## Measurements

**S933_1**

??? note "Probe code (`S933_1.py`)"
    ```python
    --8<-- "src/probe/S933_1.py"
    ```

??? note "Full run log (`S933_1_run.log`)"
    ```text
    --8<-- "src/probe/S933_1_run.log"
    ```

**S933_2**

??? note "Probe code (`S933_2.py`)"
    ```python
    --8<-- "src/probe/S933_2.py"
    ```

**Result:** asserts_passed=597 · FAIL=0

??? note "Full run log (`S933_2_run.log`)"
    ```text
    --8<-- "src/probe/S933_2_run.log"
    ```

**S935**

??? note "Probe code (`S935.py`)"
    ```python
    --8<-- "src/probe/S935.py"
    ```

**Result:** asserts_passed=95 · FAIL=0

??? note "Full run log (`S935_run.log`)"
    ```text
    --8<-- "src/probe/S935_run.log"
    ```

**S937**

??? note "Probe code (`S937.py`)"
    ```python
    --8<-- "src/probe/S937.py"
    ```

**Result:** asserts_passed=65 · FAIL=0

??? note "Full run log (`S937_run.log`)"
    ```text
    --8<-- "src/probe/S937_run.log"
    ```

**S940**

??? note "Probe code (`S940.py`)"
    ```python
    --8<-- "src/probe/S940.py"
    ```

**Result:** asserts_passed=39 · FAIL=0

??? note "Run log (paraphrased under the word fence — see provenance) (`S940_run.log`)"
    ```text
    --8<-- "src/probe/S940_run.log"
    ```


---

[← all nodes](index.md)
